import React from 'react'

const Title = ({text}) => {
  return (
    <div>
      <div className='py-5 text-center font-semibold text-2xl font-serif text-stone-800'>{text}</div>
    </div>
  )
}

export default Title
